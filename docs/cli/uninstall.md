---
summary: "CLI reference for `secureclaw uninstall` (remove gateway service + local data)"
read_when:
  - You want to remove the gateway service and/or local state
  - You want a dry-run first
title: "uninstall"
---

# `secureclaw uninstall`

Uninstall the gateway service + local data (CLI remains).

```bash
secureclaw uninstall
secureclaw uninstall --all --yes
secureclaw uninstall --dry-run
```
